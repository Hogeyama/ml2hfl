(*
let f_1078 (x_1018:((unit * unit) * (unit * (int * int)))) =
  snd x_1018
 *)


let f_1078 (x_1018:((unit * unit) * (unit * (int * int)))) =
  snd x_1018


(*
let f (xy,(z,w)) =
  xy

let main n =
  let p = (n,n) in
  n, f (p,p)
 *)
